<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nindo.manga.reader" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.WithoutActionBar" >
        <activity
            android:name=".home_activity.HomeActivity"
            android:label="@string/title_activity_code_lab"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login_activity.LoginActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".manga_details.MangaDetailsActivity"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
